export function SiteFooter() {
  return (
    <footer className="mt-auto border-t border-border/80">
      <div className="mx-auto flex w-full max-w-6xl flex-col gap-1 px-5 py-8 text-sm text-muted-foreground sm:flex-row sm:items-center sm:justify-between sm:px-6">
        <p>Dog Names. A starter catalog you can actually shout.</p>
        <p>Shortlists stay on this device for now.</p>
      </div>
    </footer>
  );
}
